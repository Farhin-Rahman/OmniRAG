# Dependencies for backend/mcp_server.py only.
#
# This runs on the HOST, not inside Docker — MCP clients (e.g. Claude
# Desktop) spawn MCP servers as local subprocesses over stdio, which
# doesn't fit a containerized process. Install into the same venv you use
# to run host-side scripts (e.g. .venv at the repo root):
#
#   .venv/Scripts/python.exe -m pip install -r backend/requirements-mcp.txt
#
# firebase-admin is already a Docker dependency (requirements-prod.txt) but
# is listed here too since this file is meant to be installable standalone.
mcp[cli]
firebase-admin
